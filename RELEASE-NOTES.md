# 发布说明

## v1.7.11 · 2026-09-10

- 修复无对话时的全局绑定保存和状态提示。全局设置不要求打开角色或对话；角色操作需要选中角色，对话操作需要打开对话。
- 区分“默认绑定”和“当前对话生效”。修改全局默认不会覆盖已有对话保存的绑定，也不会强行改写数据库当前角色的世界书配置。
- 修复数据库“酒馆当前全部”按钮的样式、选中状态与手机文字显示；脚本卸载时恢复原生按钮。
- 修复世界书同步漏掉提前到达的数据库恢复回调，以及切换对话后旧界面延迟回调可能继续应用的问题。
- 最新在线版改为查询 jsDelivr 版本列表，再加载固定正式版本；成功获取的版本缓存十分钟。dev 使用独立的测试版本筛选，不混入正式版。

本轮数据库 spv9.2.5 基本实机检查覆盖无对话全局保存、两条对话、数据库配置读回、已有模板保留、手动重复同步和两档手机布局。演示对话没有表格行，未测试非空表格写入或真实模型生成。正式源码相对实测 dev.4 仅修改版本号，正式包 54 项测试通过。

发布后的在线验收已完成：清除版本缓存后，正式通道选中 1.7.11，dev 通道选中 1.7.11-dev.4；真实 CDN 下载、哈希校验和酒馆助手环境运行均通过。重复加载保持原缓存时间戳。十分钟是加载器本地缓存期限，不是 CDN 收录新版本的时限。

升级时需要更换旧在线加载器，才能使用新的版本查询方式；只刷新旧加载器不会改变它的实现。保留原脚本数据、身份与启停状态，步骤见[升级说明](docs/USAGE.md)。安装 JSON 仍只在社区分发。

## v1.7.10 · 首次公开发行

数据库三层绑定补丁，把常用的世界书与预设配置按全局、角色和对话分别管理。

## 本次功能

- 写入世界书、填表读取世界书、剧情推进读取世界书及推进预设的分层绑定。
- 读取范围可选“酒馆当前使用的全部世界书”；手动选择时可在面板内筛选世界书与条目。
- 角色填表预设支持随卡内嵌、复制到全局、从全局复制、删除及绑定。
- 角色模板可合并全局模板，同名表格以角色模板为准；手动重应用可有序合并多个全局或内嵌模板。
- 普通对话模板交给数据库原生管理；已有模板不因切换对话被补丁自动替换。
- 提供手动清空并更新写入世界书，重复点击不并发启动。
- 数据库运行环境定位不依赖安装 UUID 或脚本名称。

## 获取方式

酒馆助手 JSON 目前仅在类脑发布帖通过 `/下载` 提供。GitHub 只提供源码、教程和在线加载所需的 JS，不提供安装 JSON 或安装包附件。

离线、固定在线和 latest 在线使用同一个脚本身份，三选一，不要同时安装。
推荐需要稳定行为的用户选择固定版；latest 会跟随新的语义化版本，并可能有 CDN 缓存延迟。

基本实机验证数据库 spv9.2.3；其他已核对版本及验证限制见[兼容性表](COMPATIBILITY.md)。
在线版启动需要连接 jsDelivr，latest 更新可能有缓存延迟。

## 注意

升级前备份脚本数据、角色卡、聊天与相关世界书。维护旧安装时保留用户数据和脚本身份，不用空 data 覆盖已有配置。
手动世界书清空只针对数据库生成条目，但属于写操作，首次使用请在备份或测试对话中验证。
界面操作和迁移步骤见[使用指南](docs/USAGE.md)。
