# v1.7.10 首次公开发行

数据库三层绑定补丁，把常用的世界书与预设配置按全局、角色和对话分别管理。

## 本次功能

- 写入世界书、填表读取世界书、剧情推进读取世界书及推进预设的分层绑定。
- 读取范围可选“酒馆当前使用的全部世界书”；手动选择时可在面板内筛选世界书与条目。
- 角色填表预设支持随卡内嵌、复制到全局、从全局复制、删除及绑定。
- 角色模板可合并全局模板，同名表格以角色模板为准；手动重应用可有序合并多个全局或内嵌模板。
- 普通对话模板交给数据库原生管理；已有模板不因切换对话被补丁自动替换。
- 提供手动清空并更新写入世界书，重复点击不并发启动。
- 数据库运行环境定位不依赖安装 UUID 或脚本名称。

## 获取方式

酒馆助手 JSON 目前仅在类脑发布帖通过 `/下载` 提供。GitHub 只提供源码、教程和在线加载所需的 JS，不提供安装 JSON 或安装包附件。

离线、固定在线和 latest 在线使用同一个脚本身份，三选一，不要同时安装。
推荐需要稳定行为的用户选择固定版；latest 会跟随新的语义化版本，并可能有 CDN 缓存延迟。

基本实机验证数据库 spv9.2.3；其他已核对版本及验证限制见[兼容性表](COMPATIBILITY.md)。
在线版启动需要连接 jsDelivr，latest 更新可能有缓存延迟。

## 注意

升级前备份脚本数据、角色卡、聊天与相关世界书。维护旧安装时保留用户数据和脚本身份，不用空 data 覆盖已有配置。
手动世界书清空只针对数据库生成条目，但属于写操作，首次使用请在备份或测试对话中验证。
界面操作和迁移步骤见[使用指南](docs/USAGE.md)。
