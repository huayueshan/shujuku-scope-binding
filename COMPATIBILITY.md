# 兼容性版本表

[compatibility.json](compatibility.json) 是机器可读的版本映射。按补丁版本记录数据库的确切标签、源码提交、验证等级及限制，不把一个版本的测试推及整段版本区间。

## 首次发行候选

首发版本为 **v1.7.10**，对应标签 `v1.7.10`。本表、JSON、源码及产物随版本冻结，不沿用其他源码版本的验收结论。

| 补丁版本 | 数据库版本 | 本候选的验证等级 | 说明 |
| --- | --- | --- | --- |
| v1.7.10 候选 | spv8.4 | 进行过基本测试，没有进行实机测试 | 基本测试通过 |
| v1.7.10 候选 | spv8.9.2 | 进行过基本测试，没有进行实机测试 | 基本测试通过 |
| v1.7.10 候选 | spv9.1 | 进行过基本测试，没有进行实机测试 | 基本测试通过 |
| v1.7.10 候选 | spv9.1.6 | 进行过基本测试，没有进行实机测试 | 基本测试通过 |
| v1.7.10 候选 | spv9.2.3 | 进行过基本测试和实机测试 | 测试通过，具体范围见下方说明 |
| 任意 | 未列出版本 | 未验证 | 包括中间版本、更新版本；不等于已知不兼容 |

spv9.2.3 进行过基本测试和实机测试；spv8.4、spv8.9.2、spv9.1、spv9.1.6 只进行过基本测试。不能据此保证从 8.4 到最新版本都兼容。

## 验证边界

- `source-contract`：对固定上游源码快照核对 API、配置与兼容分支，并通过相关自动测试；不是完整酒馆运行环境测试。
- `live-basic`：在上述基础上，使用当前候选临时注入实际环境完成基本检查；不是所有功能、第三方脚本组合或公开升级渠道的完整验收。
- 本次 spv9.2.3 实测覆盖数据库实例定位、同参数保存、两条已有对话、刷新后重入、五类绑定核验，以及 390x844 / 360x800 面板显示。未进行真实 AI 生成，也未永久替换已安装补丁。
- 旧 8.x 离线包版本头可能无法识别，依赖已有配置结构回退，不将旧头部数字视为真实数据库版本。
- SillyTavern 实测版本为 1.18.0；早期实测未记录助手版本，后续干净环境已补验酒馆助手 4.8.19，但不据此宣称它的最低兼容版本。
- 离线、固定在线和 latest 在线产物由同一构建器生成。在线模块可以在本地映射到实际打包源码验收，但不能把该验收当成 CDN 已发布或缓存已经刷新。
- 干净环境另发现同步按钮浅底浅字，追加既有按钮样式类后已临时加载复验。测试核对当前正文摘要与最近实机检查记录；改变可执行源码仍须重新验收。

## 后续维护

1. 每次 release 增加自己的记录，保存补丁源码 SHA-256、数据库标签及提交号；旧 release 对应旧记录，不覆盖历史结论。
2. 新数据库版本先加入“未验证”的评估范围。完成源码契约检查和测试后，才标记对应验证等级。
3. 更改补丁源码后必须重新验收，不能只修改 SHA-256 让测试通过。当前候选的版本与源码摘要由自动测试核对。
4. 最新在线入口应落到某个具体 release，并携带该 release 的映射；latest 不意味着兼容未来的数据库。
5. 已知不兼容应明确记录故障与替代版本，不用“未验证”掩盖已知问题。离线、固定在线、latest 的交付验证分别记录。
