# dev 测试版

当前版本：`1.7.11-dev.1`。本分支用于测试，不是正式 release。正式版本及其兼容性记录请看 `main`；本分支保留的介绍页面和历史兼容性表仍介绍 v1.7.10。

## 本次修复

- 全局绑定不要求打开角色或对话；没有对话时只保存默认设置，不执行对话同步。
- 角色绑定缺少角色、对话绑定或手动合并缺少对话时，明确提示并停止操作。
- 修复数据库 spv9.2.3 在无对话时切换全局表格预设失败的问题。正常情况仍使用数据库公开 API；仅遇到明确的聊天元数据未就绪错误时，按已核对的数据库配置结构一起保存全局模板及名称。失败或切换上下文时不会继续写入。
- 已有对话保留数据库自己的模板。

## 验证范围

本地相关测试 150 项通过，覆盖逻辑及 spv8.4、spv8.9.2、spv9.1、spv9.1.6、spv9.2.3 的已维护检查。旧版本未重新进行实机测试。

在隔离的 SillyTavern 1.18.0、酒馆助手 4.8.19、数据库 spv9.2.3 中完成临时注入测试：无角色、无对话的全局保存，缺少上下文的提示，刷新后的数据保留，两条已有对话及 390×844、360×800 布局。

另外直接读取数据库的全局模板、当前推进预设和原生设置，核对写入目标与选书列表；第二条对话的 8 张模板表与数据库实际运行表格的名称、列和 SQL 结构一致。测试未进行模型生成，也不是安装或历史版本数据迁移测试。

## 构建

运行 `npm run build` 只生成 `dist/index.js` 与对应摘要，不生成或上传安装 JSON。`npm test` 运行公开快照中的相关逻辑测试；150 项完整测试还包括维护仓库中的上游版本检查。

dev 地址受 CDN 缓存影响，需核对 `dist/manifest.json` 的版本和摘要；已经运行的页面不会自动热更新。正式 main、版本标签和 latest 不随 dev 修改。
