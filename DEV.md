# dev 测试版

当前测试快照：`1.7.11-dev.4`。使用固定测试标签，不再推进可变 dev 地址。正式版本及其兼容性记录请看 `main`；历史介绍页面仍介绍 v1.7.10。

## dev.4 验证

- 新加载器从 jsDelivr 查询固定版本，正式/测试通道分开，查询结果缓存十分钟。详见 [在线版本选择](docs/VERSION-CHANNELS.md)。
- 修复原生世界书来源选项的 scoped CSS、手机文字省略、切换选中状态及热更新清理。
- 数据库控件延迟回调在对话切换后丢弃，没有对话不写聊天绑定。
- spv9.2.5 隔离实机：五类全局保存、两条已有对话、独立读取数据库写入目标和选书列表、手动同步幂等与表格保持均通过。
- 390×844、360×800 补丁面板未越界；原生来源按钮44px高、边框0px，手动/当前全部来回切换正确。
- 165项维护仓库测试、54项公开快照测试通过。没有测试真实模型生成、损坏历史恢复或非空表格同步；不把这些结果外推到所有数据库版本。

下面保留早期测试记录。

dev.3 修复了实机发现的世界书同步等待问题：数据库有数据的恢复回调可能早于补丁的等待窗口，原来会漏掉这次就绪信号。现在保留该回调，等待上下文、持久化及稳定时间检查通过后再同步；切换时的空数据清理回调不能充当就绪信号。

## 本次修复

- 全局绑定不要求打开角色或对话；没有对话时只保存默认设置，不执行对话同步。
- 角色绑定缺少角色、对话绑定或手动合并缺少对话时，明确提示并停止操作。
- 修复数据库 spv9.2.3 在无对话时切换全局表格预设失败的问题。正常情况仍使用数据库公开 API；仅遇到明确的聊天元数据未就绪错误时，按已核对的数据库配置结构一起保存全局模板及名称。失败或切换上下文时不会继续写入。
- 已有对话保留数据库自己的模板。

## 验证范围

本地相关测试 155 项通过，覆盖逻辑及 spv8.4、spv8.9.2、spv9.1、spv9.1.6、spv9.2.3、spv9.2.5 的已维护检查。旧版本未重新进行实机测试。

### spv9.2.5 检查

已在隔离酒馆完成基本实机测试：没有角色或对话时保存五类全局设置，缺少上下文时阻止角色/对话操作，刷新后读回数据库的模板和推进预设，两条已有对话的自动同步，以及手动同步和重复调用。直接核对数据库 API 和原生设置，没有只看补丁提示。关闭对话后待同步任务清除，未观察到后续刷新。

390×844、360×800 面板均未越界；已有对话模板没有被手动世界书同步修改。演示对话没有表格行，世界书仍为空，因此本轮验证的是同步流程完成，不包含非空表格内容写入、模型生成或损坏历史恢复。

上游固定提交：`5c53f795832b194ec4baa74135e8ebd950122438`。模板、推进预设、世界书清理同步和回调 API 与 9.2.3 一致；按角色保存世界书配置、聊天模板存储及延迟初始化方式保留。全局模板的无对话兼容分支仍适用。

新版在 V2 历史只能兼容读取时拒绝切换模板；补丁保留该失败结果，不绕过数据库的恢复要求。新增三项本地检查覆盖这些边界。没有在损坏历史上进行实机恢复测试。

测试时建议先备份，核对：未打开对话时保存全局设置；进入对话后数据库实际选择是否正确；切换已有对话是否保留各自模板；世界书同步是否完成。不要同时用多个页面操作不同对话的共享写入世界书。

此前 dev.1 在隔离的 SillyTavern 1.18.0、酒馆助手 4.8.19、数据库 spv9.2.3 中完成临时注入测试：无角色、无对话的全局保存，缺少上下文的提示，刷新后的数据保留，两条已有对话及 390×844、360×800 布局。该证据不等于 9.2.5 实机通过。

另外直接读取数据库的全局模板、当前推进预设和原生设置，核对写入目标与选书列表；第二条对话的 8 张模板表与数据库实际运行表格的名称、列和 SQL 结构一致。测试未进行模型生成，也不是安装或历史版本数据迁移测试。

## 构建

运行 `npm run build` 只生成 `dist/index.js` 与对应摘要，不生成或上传安装 JSON。`npm test` 运行公开快照中的 52 项相关逻辑测试；155 项完整测试还包括维护仓库中的上游版本检查。

旧可变 dev 地址停止推进；新加载器选择固定测试标签。正式 main 和已有版本标签不随测试修改。
