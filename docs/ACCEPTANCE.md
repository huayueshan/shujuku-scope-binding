# v1.7.10 候选验收

检查日期：2026-09-08。本记录不是公开发布声明。

## 已有证据

| 范围 | 结果与边界 |
| --- | --- |
| 公开仓库产品与发行测试 | 59 项通过，包含独立逻辑、离线执行、固定在线和 latest 模块加载、归档反向测试、许可声明及静态文档站；不访问 CDN |
| 开发库完整回归 | 148 项通过，包含固定上游快照契约与内部脱敏扫描器测试；与公开测试有重叠，不能相加当成独立用例总数 |
| 兼容性 | [五个固定数据库版本](../COMPATIBILITY.md)分别记录等级；仅 spv9.2.3 有本候选基本实机证据 |
| 产物一致性 | 三种 JSON 共用固定 UUID；离线正文与源码相同；每个发行载荷均有 SHA-256 和字节数；不附用户 data |
| 在线模块 | 两个加载器在隔离 JavaScript 环境中导入实际 dist/index.js；只验证模块内容和执行，不代表 CDN 路由成功 |
| 归档 | 内存 ZIP 往返校验全部白名单文件；两次生成摘要相同；本地候选 ZIP 与验证摘要一致，尚未公开分发 |
| 许可 | PolyForm Noncommercial 1.0.0 官方正文，源码和三个交付渠道均附必要声明；LICENSE/NOTICE 摘要也纳入构建验证 |
| 拒绝路径 | 未确认许可、构建后篡改、重复路径、目录越界、清单缺项及许可不一致均会失败 |
| 脱敏 | 白名单文本、文件名、JSON、常见转义与编码扫描未命中私人标识和凭据规则；不是任意编码或所有潜在隐私的数学保证 |
| Git | 初始历史、索引和对象库为空；有效作者/提交者为公开身份；尚未创建提交或 tag |

## 仍需完成

- 干净环境已补录酒馆助手 4.8.19，但不支持最低版本承诺。
- 离线首次安装及旧候选保留数据更新已完成，见下节；未覆盖所有历史版本的数据迁移。
- 获得发布批准后才创建版本快照、tag 和 Release；上传后再核验固定/CDN latest 实际内容及缓存。

许可证、源代码或交付结构改变后，本记录不能直接沿用为最终发行结论。兼容性测试会核对去除许可头后的正文与最近一次记录的实机源码摘要，不允许后续修改静默沿用旧证据。

### 干净演示环境补验

2026-09-08：SillyTavern 1.18.0、酒馆助手 4.8.19、数据库 spv9.2.3，空用户数据启动，使用虚构演示角色，不连接模型 API。全局世界书来源可保存，新对话继承并固化后自检 5/5。无有效对话的首页曾显示 4/5，不能拿首页状态替代对话验收。

默认主题暴露了手动更新按钮缺少样式类导致浅底浅字的问题；补上既有命令按钮样式后临时加载完整候选复验。390x844、360x800 下分别为 378x832、348x788，无横向溢出，按钮恢复深底浅字。此项不代表完成公开导入、保留用户数据升级或 CDN 在线加载验收。

## 社区发帖阶段

### 离线安装与保留数据更新

2026-09-08，在上述隔离酒馆移除补丁后，使用酒馆助手真实文件导入处理器导入最终 `dist/offline.json`，再启用。文件选择由自动化提供，JSON 解析、创建脚本、启停和保存使用酒馆助手原逻辑。只生成一个脚本，导入默认关闭且重新生成 ID；启用后进入演示对话，核验 5/5，磁盘正文与发行源码一致。

更新测试使用演示环境原有的旧 v1.7.10 候选配置，只替换最终候选正文，保持非空 `data`。冷启动后比较页面与磁盘：全局绑定、UI 状态、ID、启停、按钮及导出配置均不变；角色数据、对话变量、原生模板摘要与更新前一致，非目标脚本也不变。核验为 5/5，最后错误为空。

这证明本次离线安装和保留数据更新可用，不代表所有历史版本迁移、再次导入覆盖、在线 API 更新或 CDN 加载均已验收。测试后恢复演示环境原配置并停服，没有改动日常酒馆，也没有上传发布。

图文站已附桌面、390x844、360x800 三张合成演示截图，图片原始字节及无 EXIF/注释元数据检查通过；不使用私人实机画面。类脑发帖前仍可更换演示内容，但替换图片须重新审查，不阻塞脚本 JSON 的发行准备。
另补三张步骤局部图：专用世界书的全局绑定、当前全部读取选项、角色模板合并开关。来自同一隔离酒馆，实际创建并绑定虚构世界书、保存角色合并并生成数据库原生对话模板；没有调用模型。图像裁切由浏览器截图完成，没有修改文字或勾选状态。
面向用户的主要发行资产始终是离线、固定在线、latest 三个酒馆助手 JSON；源码 ZIP 只是附加资产。

## 图文站本地检查

静态 [使用指南](../index.html) 的布局已在内置浏览器检查桌面、390x844、360x800；目录跳转、手机目录折叠、FAQ 展开与三张图片加载正常，无横向溢出。后续文案改为专用世界书、当前世界书读取、角色模板合并三项使用说明，并移除下载入口；三种 JSON 发行产物保留。这是本地预览，不是 GitHub Pages 或 CDN 上线验证。版本与兼容性表由发行数据生成，没有启用 Pages、创建提交或 push。
后续源码核查已明确原生能力与补丁增量，补充填表 `$4`、剧情 `$1` 和隔离清理边界；详见 [行为核查记录](BEHAVIOR.md)。

后续用语统一为“数据库二创”和“用户设定（人设）”，兼容性表直接说明是否进行过基本测试、实机测试。写入示例改为“数据库表格专用世界书”，补充两条演示对话之间切换后的真实同步开始和完成提示。截图时只延长通知停留时间，随后恢复；没有调用模型或访问日常酒馆。

脚本面板的原 `Persona / Avatar` 标签也已改为“用户设定（人设）”；接口名称和存储字段不变。最终标签修订在隔离酒馆临时加载，桌面及 390x844、360x800 均可见，重新拍摄四张总览和设置图。临时启动时曾出现数据库就绪等待重试，随后面板显示绑定核验 5/5；这次标签检查不代表重新完成全部同步时序测试。此前真实切换对话的通知图与本次标签截图分别记录，不混为同一次验收。
